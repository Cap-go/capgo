Task 5 Evidence - Create SSO Provider CRUD Endpoints
Date: 2026-02-23

Implemented files:
- supabase/functions/_backend/private/sso/providers.ts (new)
- supabase/functions/private/index.ts (route registration)

Endpoint coverage:
- POST /private/sso/providers
  - Validates body with Zod
  - Checks org.manage_sso permission
  - Enforces Enterprise plan via requireEnterprisePlan
  - Calls createSSOProvider Management API
  - Generates 32-char hex dns_verification_token
  - Inserts row with status=pending_verification
  - Returns sanitized provider (no dns_verification_token)

- GET /private/sso/providers/:orgId
  - Validates orgId UUID
  - Checks org.manage_sso permission
  - Lists org providers
  - Returns sanitized providers (no dns_verification_token)

- PATCH /private/sso/providers/:id
  - Validates provider id UUID
  - Loads provider to resolve org for permission check
  - Allows metadata_url, attribute_mapping, enforce_sso updates only
  - Does not allow domain updates
  - Returns sanitized provider

- DELETE /private/sso/providers/:id
  - Validates provider id UUID
  - Loads provider to resolve org for permission check
  - Deletes DB row first
  - Calls deleteSSOProvider Management API only when provider_id exists and DB delete succeeded
  - Returns BRES JSON response

Verification:
- lsp_diagnostics: clean
  - supabase/functions/_backend/private/sso/providers.ts
  - supabase/functions/private/index.ts

- lint/build:
  - bun lint:backend -> passed
  - bun run build -> passed
