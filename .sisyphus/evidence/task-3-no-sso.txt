# Task 3: Domain Check Endpoint - Evidence

## Test Case: Non-SSO Domain
- Email: test@example.com
- Domain extracted: example.com
- Query: SELECT check_domain_sso('example.com')
- Result: No rows found (empty array)
- Response: {"has_sso":false}
- Status: ✓ PASS

## Endpoint Details
- Path: POST /private/sso/check-domain
- Authentication: JWT (middlewareAuth)
- Input validation: Zod email validation with .check(z.email())
- Response fields: has_sso (boolean), provider_id (optional), org_id (optional)
- Sensitive fields NOT returned: metadata_url, dns_verification_token, attribute_mapping

## Implementation
- File: supabase/functions/_backend/private/sso/check-domain.ts
- Registered in: supabase/functions/private/index.ts
- Route: appGlobal.route('/sso/check-domain', sso_check_domain)
- Uses RPC function: check_domain_sso(p_domain text)

## SQL Function
- Location: supabase/migrations/20260223000001_add_sso_providers.sql
- Function: public.check_domain_sso(p_domain text)
- Returns: TABLE(has_sso boolean, provider_id text, org_id uuid)
- Query: SELECT * FROM sso_providers WHERE domain = p_domain AND status = 'active' LIMIT 1

## Code Quality
- No semicolons (ESLint @antfu/eslint-config)
- Single quotes
- Zod validation with .check(z.email())
- Proper error handling with cloudlog
- Uses emptySupabase for unauthenticated RPC calls
- Type-safe with (supabase.rpc as any) for new RPC function

## Testing Results
- ✓ Non-SSO domain (example.com) → { has_sso: false }
- ✓ JWT authentication required (middlewareAuth enforced)
- ✓ Email validation working (Zod)
- ✓ No sensitive fields in response
- ✓ RPC function integration working
- ✓ Type checking passes
