TASK 8: Enterprise Plan Validation Utility
============================================

FILE CREATED: supabase/functions/_backend/utils/plan-gating.ts

IMPLEMENTATION SUMMARY:
- Exports requireEnterprisePlan(c, orgId) - throws 403 if org not on Enterprise
- Exports hasFeature(c, orgId, feature) - checks if org has feature enabled
- Uses getCurrentPlanNameOrg() RPC to fetch current plan (no caching)
- Proper error handling with cloudlog/cloudlogErr
- Follows Hono Context pattern consistent with other utilities

PLAN NAMES DISCOVERED:
From supabase/functions/_backend/utils/plans.ts planToInt() function:
- Solo = 1
- Maker = 2
- Team = 3
- Enterprise = 4

Enterprise plan name: 'Enterprise' (exact string match required)

FUNCTION SIGNATURES:
```typescript
export async function requireEnterprisePlan(c: Context, orgId: string): Promise<void>
export async function hasFeature(c: Context, orgId: string, feature: string): Promise<boolean>
```

ERROR HANDLING:
- requireEnterprisePlan throws quickError(403, 'enterprise_plan_required', ...)
- hasFeature returns false on error (graceful degradation)
- All errors logged with requestId for tracing

FEATURE MAPPING:
- 'sso' feature requires Enterprise plan
- Extensible for future features via featureRequirements map

CODE QUALITY:
✓ No semicolons (ESLint @antfu/eslint-config)
✓ Single quotes
✓ Proper TypeScript types
✓ JSDoc docstrings for public API
✓ Consistent with existing utility patterns
✓ Linting passes (plan-gating.ts clean)

INTEGRATION READY:
- Can be imported: import { requireEnterprisePlan, hasFeature } from '../utils/plan-gating.ts'
- Ready for use in SSO provider endpoints
- No external dependencies beyond existing utilities
